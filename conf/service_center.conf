# Service discovery center address.
service_center_ip = 127.0.0.1

# Port used by clients to query RPC service locations.
query_port = 8080

# Port used by runtime commands to add, delete, or modify service entries.
control_port = 9090

# Registered service nodes are ignored after this TTL without add/heartbeat.
service_ttl_ms = 10000
